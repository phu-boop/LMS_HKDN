# LMS_HKDN

## Frontend setup

Requirements:
- Node.js 20+ and npm (LTS recommended)

Install and run:
```
cd frontend
npm install
npm run dev
```

Notes:
- `npm audit` reports a moderate warning from `postcss` nested in `next`. This is safe to ignore during local development and will be fixed when Next.js ships an update.
