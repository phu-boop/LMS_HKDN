/** PDF: id → URL nguồn chỉ dùng trên server (ưu tiên file cục bộ trong documentPdfLocal nếu có) */
export const PDF_REMOTE_BY_ID: Record<string, string> = {
  demo: 'https://mozilla.github.io/pdf.js/web/compressed.tracemonkey-pldi-09.pdf',
};
